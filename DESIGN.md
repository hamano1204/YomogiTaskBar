# YomogiTaskBar 設計書

## 1. システム概要

### 1.1 目的
YomogiTaskBarは、Windows用の垂直型タスクバーアプリケーションです。起動中のアプリケーションを仮想デスクトップごとに一覧表示し、マウス操作またはキーボードショートカットによる高速なアプリ切り替えを可能にします。

### 1.2 主な機能
- **起動中アプリの一覧表示**: 仮想デスクトップごとに実行中のウィンドウを整理して表示。
- **直感的な操作**: クリックによるアクティブ化、ホバー時の「✕」ボタンによるウィンドウ終了。
- **キーボードナビゲーション**: 
  - `Win + Esc` でタスクバーをアクティベート。
  - 上下キーで選択、`Enter` で決定。
  - 各種操作ホットキー（最小化、最大化、閉じる）。
- **マルチモニター対応**: 
  - メインモニターだけでなく、全モニターのウィンドウを一本のリストに統合。
  - サブモニター上のウィンドウには識別用のカラーインジケーターを表示。
- **AppBar機能**: 画面端（左右）へのドッキング、他のウィンドウを避ける領域予約、自動隠蔽モード。
- **テーマ対応**: ライト/ダーク/システム設定への追従。
- **スタートアップ**: Windows起動時の自動実行。

### 1.3 技術スタック
- **フレームワーク**: .NET 9.0
- **UI**: WPF (Windows Presentation Foundation)
- **言語**: C# 12.0
- **ターゲット**: Windows 10 (Build 19041.0) 以降
- **主要な依存関係**: 
  - Windows API (P/Invoke)
  - Windows.Management.Deployment (UWPアプリ情報取得用)

---

## 2. アーキテクチャ

### 2.1 全体構成
```
┌─────────────────────────────────────────────────────────┐
│                      App.xaml                           │
│              (アプリケーションエントリーポイント)         │
│              App.xaml.cs (多重起動防止)                   │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│                  MainWindow.xaml                         │
│  ┌──────────────────────────────────────────────────┐  │
│  │            MainWindow.xaml.cs                     │  │
│  │  ┌────────────────────────────────────────────┐  │  │
│  │  │  Controllers                              │  │  │
│  │  │  - AppBarController (ドッキング制御)        │  │  │
│  │  │  - WindowStateManager (状態永続化)         │  │  │
│  │  └────────────────────────────────────────────┘  │  │
│  │  ┌────────────────────────────────────────────┐  │  │
│  │  │  Managers                                  │  │  │
│  │  │  - WindowManager (ウィンドウ列挙・操作)      │  │  │
│  │  │  - SettingsManager (設定ファイル管理)       │  │  │
│  │  │  - HotkeyListener (グローバルホットキー)    │  │  │
│  │  │  - StartupManager (スタートアップ管理)      │  │  │
│  │  └────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                     │
         ┌───────────┼───────────┐
         ▼           ▼           ▼
┌─────────────┐ ┌─────────┐ ┌──────────────┐
│  ViewModels │ │ Models  │ │  Utilities   │
│             │ │         │ │              │
│ WindowItem  │ │Settings │ │ NativeMethods│
│ ViewModel   │ │         │ │ Logger       │
└─────────────┘ └─────────┘ └──────────────┘
```

---

## 3. コンポーネント詳細

### 3.1 MainWindow (TaskBar UI)
**役割**: メインタスクバーのUI表示、ユーザー操作のハンドリング。
- **仮想デスクトップ表示**: 全デスクトップのウィンドウをセパレーターで区切って一括表示。
- **更新ロジック**: `DispatcherTimer` により一定間隔（デフォルト2秒）でリストをリフレッシュ。
- **ホバーアクション**: アイテム上にマウスを置くと「閉じる（✕）」ボタンが表示されるミニマルな設計。

### 3.2 WindowManager
**役割**: OSレベルのウィンドウ情報を抽出し、アプリが扱いやすい形式に変換。
- **列挙**: `EnumWindows` を使用し、タスクバーに表示すべき有効なウィンドウを収集。
- **フィルタリング**: 特殊なシステムウィンドウ、クロークされたウィンドウ、異常サイズなどを除外。
- **アイコン取得**: 
  - 標準Win32アプリ: `WM_GETICON` または実行ファイルから抽出。
  - UWPアプリ: `PackageManager` を使用してアプリパッケージからLogoを取得。
- **ソート**: 仮想デスクトップ順 → モニター順 → プロセスID順 で一貫した並びを提供。

### 3.3 AppBarController
**役割**: Windowsのデスクトップ領域予約（AppBar）機能の管理。
- **ドッキング**: 画面の左右いずれかに配置され、他のアプリと重ならない領域を確保。
- **自動隠蔽**: ピン留め解除時、マウスが離れると自動的に画面端に隠れ、戻ると再表示。

### 3.4 Settings & Persistence
- **SettingsManager**: `%APPDATA%\YomogiTaskBar\settings.json` に設定を保存。
- **WindowStateManager**: ウィンドウの幅、配置エッジ、モニター位置などを次回起動時も維持。

---

## 4. データモデル

### 4.1 AppSettings
- **ThemeMode**: "System", "Light", "Dark"
- **LaunchOnStartup**: スタートアップ起動の有効/無効
- **GlobalActivate**: タスクバー呼び出し（Win+Esc）
- **Minimize / ToggleMaximize / Close**: 選択中アイテムへの操作ホットキー

### 4.2 WindowItemViewModel
- **MonitorIndex**: ウィンドウが存在するモニター番号。
- **ShouldShowLeftIndicator**: マルチモニター時に表示されるモニター識別カラー線（左端固定）。
- **IsDesktopSeparator**: デスクトップ区切り用の特殊アイテム判定。

---

## 5. 設定ファイル構造 (settings.json)

```json
{
  "ThemeMode": "System",
  "LaunchOnStartup": false,
  "GlobalActivate": {
    "Key": "Escape",
    "Modifiers": "Windows"
  },
  "Minimize": { "Key": "J", "Modifiers": "Control" },
  "ToggleMaximize": { "Key": "K", "Modifiers": "Control" },
  "Close": { "Key": "L", "Modifiers": "Control" },
  "WindowSettings": {
    "IsAppBarMode": true,
    "Edge": 2,
    "MonitorIndex": 0,
    "WindowWidth": 300.0,
    "LastMonitorCount": 1
  }
}
```

---

## 6. 既知の制約と考慮事項

- **UWPアプリのアイコン**: 最小化状態では汎用アイコンになることがあるため、最小化時はキャッシュを更新しない仕様。
- **安定性**: 起動時は常にピン留め（領域予約）モードで開始し、AppBar登録の競合を防止。
- **仮想デスクトップ**: 複数のデスクトップにまたがるウィンドウは「すべてのデスクトップ」グループに集約。
